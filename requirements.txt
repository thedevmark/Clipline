PySide6>=6.7
Pillow>=12.0
typing_extensions
yt-dlp>=2023.0.0

# Optional — captioning runtime. Install manually into the same Python
# environment if you need the caption pass (Shorts stage prompts for it).
# faster-whisper>=1.0.0
# torch>=2.0
# pyannote.audio>=3.1
